
<div class="home banner f-flex text-center align-items-center mb-3">
  <h1 class="pt-3 pb-2" >Mister cocktail </h1>
  <i class="fab fa-redhat text-white icon"></i>
</div>
  <br>
  <br>
    <h2 class ='text-center text-white mb-5'>Choose your cocktail</h2>
  <div class="container">
    <div class="row">
      <!-- <div class="cards"> -->
        <% @cocktails.each do |cocktail| %>
        <div class="col-sm-6 col-lg-4">
          <div class="card-trip" style = "">
            <%= cl_image_tag cocktail.photo, height: 300, crop: :fill %>

            <div class="bg-white d-flex justify-content-between mx-2 card-trip-infos">
              <%= link_to cocktail.name, cocktail_path(cocktail) %>
              <%= link_to "🗑", cocktail_path(cocktail), method: :delete, class: "throw" %>
            </div>
          </div>
        </div>
        <% end %>
      <!-- </div> -->
    </div>
  </div>

    <br>
    <br>

     <div class="d-flex justify-content-center mb-5">
     <%= link_to '+Add a cocktail', new_cocktail_path, class: "btn btn-primary mr-3" %>
     <!-- <button class = "btn btn-danger toggle-trash">Delete a cocktail</button> -->
     </div>
    </div>
  </div>

<%= content_for(:after_js) do %>
  <%= javascript_pack_tag 'toggle_trash' %>
<% end %>
