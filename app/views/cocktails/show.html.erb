<h1>The <%= @cocktail.name %></h1>

<% @cocktail.doses.each_with_index do |dose, index| %>
<p><%= link_to "🗑", dose_path(dose), method: :delete  %> <%= index + 1 %>. <%= dose.description %> of <%= dose.ingredient.name %>
</p>
<% end %>

<%= link_to '⃪back to cocktails', cocktails_path, class: 'btn btn-primary'%>

<%= link_to '+ add ingredients', new_cocktail_dose_path(@cocktail) , class: 'btn btn-info'%>

